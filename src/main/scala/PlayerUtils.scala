import processing.core.PConstants

object PlayerUtils {



}

object KeyConfig extends PConstants {
  val left: Seq[Int] = Seq(PConstants.LEFT, 65)
  val up: Seq[Int] = Seq(PConstants.UP, 87)
  val right: Seq[Int] = Seq(PConstants.RIGHT, 68)
  val down: Seq[Int] = Seq(PConstants.DOWN, 83)
}
